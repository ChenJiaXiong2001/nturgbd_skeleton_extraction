# NTU RGB+D 骨架提取方法调整记录

> 本文档只记录方法调整的核心思路、调整原因和实验结果。骨架提取属于数据预处理推理，不是重新训练 YOLO 或 RTMW。

## 1. 初始方法与问题

初始骨架提取方法采用：

- 人物检测器：RTMDet-tiny；
- 姿态估计器：RTMW-L；
- 输出：每帧最多两个人，每人133个WholeBody关键点。

RTMW 是自顶向下姿态估计模型，必须先获得人物检测框。如果检测器在拥抱、握手或相向行走等双人动作中只检测到一个人，RTMW 无法恢复另一个人的骨架。

实际质量检查发现，主要问题不是 RTMW 完全失效，而是人物检测阶段不稳定：

1. 双人动作中，两人发生遮挡时可能只检测到一个人；
2. 单人动作中，背景人物有时会被误认为第二个人；
3. 检测框在相邻帧发生变化时，人物槽位可能交换或骨架发生跳变。

因此后续调整的核心原则是：**保持 RTMW-L 不变，只比较和改进人物检测阶段**。

## 2. 为什么先尝试 YOLOv8n

为了判断问题是否主要来自 RTMDet，早期使用 YOLOv8n 替换人物检测器，RTMW-L、视频和关键点处理方式保持不变。

YOLOv8n 的优势是模型较小、推理较快。在普通单人动作 A001 的30帧抽样实验中，两种方法均在全部帧检测到人物：

| 方法 | 人物检测完整性 | 平均耗时 |
|---|---:|---:|
| RTMDet + RTMW | 30/30 | 628.55 ms/帧 |
| YOLOv8n + RTMW | 30/30 | 457.61 ms/帧 |

这说明 YOLOv8n 能够处理普通单人场景，并具有一定速度优势。

但在遮挡明显的 A055（拥抱）95帧完整视频中，YOLOv8n 对第二个人的召回不足：

| 方法 | 检测到两人的帧数 | 双人召回率 | 身体完整率 |
|---|---:|---:|---:|
| RTMDet crop + RTMW | 95/95 | 100.00% | 100.00% |
| YOLOv8n crop + RTMW | 68/95 | 71.58% | 91.41% |

因此，YOLOv8n 虽然在普通单人动作中较快，但没有解决严重遮挡下的双人漏检问题，没有被选为最终修复模型。

## 3. 为什么改用 YOLO26-X

YOLOv8n 的实验表明，更换人物检测器可以改变骨架质量，但轻量模型对重叠人物的检测能力仍然不足。因此后续使用更大的 `yolo26x.pt` 作为困难样本的人物检测器，并继续使用相同的 RTMW-L 估计关键点。

为提高遮挡人物召回，重抽时采用：

- 人物置信度阈值：0.15；
- NMS IoU：0.70；
- 输入尺寸：960；
- 人物框向外扩展：10%。

这样设置的原因是：

1. 降低人物置信度阈值，减少第二个人因置信度较低而被丢弃；
2. 提高 NMS IoU，降低两个高度重叠人物框互相抑制的概率；
3. 增大输入尺寸，提高远处人物和局部遮挡人物的检测细节；
4. 扩大人物框，为 RTMW 保留更完整的身体区域。

在同一 A055 视频的95帧对比中，YOLO26-X 检测到95个双人帧：

| 方法 | 检测到两人的帧数 | 双人召回率 | 身体完整率 |
|---|---:|---:|---:|
| RTMDet crop + RTMW | 95/95 | 100.00% | 100.00% |
| YOLO26-X crop + RTMW | 95/95 | 100.00% | 100.00% |

按人物框匹配后，YOLO26-X 与 RTMDet 的平均框 IoU 为0.9015，身体关键点平均位置差异为2.925像素。这说明两种方法提取到的人体位置基本一致。

检测器输出的人物顺序并不稳定，因此不能直接把“第一个框”固定视为同一个人。最终方法使用跨帧位置匹配维护两个人物槽位。

## 4. 困难样本重抽结果

从原始失败结果中选择同一批100个困难样本，分别使用 RTMDet 和 YOLO26-X 重新提取，之后使用相同质量规则检查：

| 人物检测器 | 样本数 | 通过 | 失败 | 通过率 | 平均质量分 |
|---|---:|---:|---:|---:|---:|
| RTMDet-tiny | 100 | 5 | 95 | 5% | 80.521 |
| YOLO26-X | 100 | 85 | 15 | 85% | 96.149 |

YOLO26-X 将通过率从5%提高到85%，平均质量分提高15.628分。因此最终选择 YOLO26-X 对原始失败样本进行重抽，而不是重新提取所有已经合格的样本。

YOLO26-X 重抽后剩余15个失败样本的动作分布为：

- A059：13个；
- A055：1个；
- A019：1个。

A059 是双人相向行走。该动作中人物可能从远处进入画面，部分帧只出现一个完整人物，因此剩余失败不能简单归因于模型错误，也不能通过统一放宽所有双人动作阈值解决。

需要说明：这100个样本是从严重失败样本中选出的困难子集，不是从完整数据集随机抽样。因此85%表示困难失败样本的修复通过率，不能写成 YOLO26-X 在整个 NTU 数据集上的检测准确率。

## 5. 为什么加入动作人数先验

仅检查“至少检测到一个人”不能区分以下两种错误：

- 双人动作只检测到一个人；
- 单人动作把背景人物检测成第二个人。

NTU 动作标签本身包含人数先验，因此将动作划分为：

- A050–A060、A106–A120：期望2人；
- 其他动作：期望1人。

提取阶段按动作限制保留人物数：单人动作最多保留1人，双人动作最多保留2人。输出数组仍保留两个槽位，以保持下游输入维度一致。

质量检查阶段进一步判断：

- 单人动作人物召回率至少为0.95；
- 双人动作人物召回率至少为0.85；
- 连续缺少期望人物不得超过10帧；
- 单人动作出现额外人物不得超过总帧数的10%；
- 单人动作连续出现额外人物不得超过10帧；
- 身体关键点完整率至少为0.80；
- 同时检查骨架大跳变和人物槽位交换。

人数先验的作用不是提高模型本身精度，而是让错误能够被自动识别，并避免单人误检两人仍被判定为高质量。

## 6. 全量质量检查结果

在统一人数先验和质量规则下，共检查114,480个已有骨架：

| 总数 | 通过 | 失败 | 错误 | 通过率 | 平均质量分 |
|---:|---:|---:|---:|---:|---:|
| 114,480 | 104,141 | 10,339 | 0 | 90.969% | 99.053 |

平均质量分较高，但仍有10,339个样本触发至少一项硬性质量规则。这说明部分失败样本可能只是短时间缺人、出现额外人物或轨迹跳变，而不是整段骨架完全不可用。

## 7. 最终处理方法

最终方法为：

1. 首先使用统一质量规则检查全部已有骨架；
2. 保留已经通过检查的原始 RTMDet + RTMW 结果；
3. 对失败样本使用 YOLO26-X 重新检测人物，并使用相同 RTMW-L 重新提取骨架；
4. 使用完全相同的质量规则检查重抽结果；
5. 重抽结果通过时替换原失败骨架；
6. 重抽后仍然失败的样本不进入最终训练集合。

这种方法没有直接将全部数据切换到 YOLO26-X，而是把 YOLO26-X 作为困难失败样本的修复检测器。这样既保留已经稳定的结果，也把计算集中在人物遮挡、漏检和轨迹异常样本上。

## 8. 核心结论

本次调整的核心不是更换姿态模型，而是发现并解决人物检测对自顶向下骨架提取的限制：

1. RTMW 的主要失败来源之一是上游人物框缺失；
2. YOLOv8n 在普通单人动作中较快，但严重遮挡双人召回不足；
3. YOLO26-X 配合较低置信度、较高 NMS IoU 和更大输入尺寸，显著提高了困难失败样本的修复通过率；
4. 动作人数先验能够同时发现“双人漏检”和“单人误检多人”；
5. 统一质量规则保证原始结果与重抽结果使用同一标准比较。

正式发表前还需要补充 YOLO26-X 全量修复后的成功修复数、最终剔除数和各动作类别的最终保留数量。删除失败样本可能改变类别分布，因此必须在论文中透明报告。
